pluginManagement {
  repositories {
    maven { url "${ System.getenv('REPOSITORY_MANAGER_URL') }/ossim-deps" }
    gradlePluginPortal()
  }
}

include 'omar-openlayers-plugin'

project(':omar-openlayers-plugin').projectDir = "${rootProject.projectDir}/plugins/omar-openlayers-plugin" as File
